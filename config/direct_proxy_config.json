{
  "proxy_config": {
    "name": "Direct LLM Proxy",
    "description": "Direct connection to LLM providers with server-side context management",
    "base_url": "http://127.0.0.1:${PROXY_PORT}",
    "endpoints": {
      "anthropic": "/v1/messages",
      "openai": "/v1/chat/completions",
      "models": "/v1/models",
      "health": "/healthz"
    },
    "models": {
      "research": {
        "id": "your-research-model",
        "display_name": "Research Model",
        "description": "Research model with server-side context management",
        "endpoint": "openai"
      },
      "general": {
        "id": "your-general-model",
        "display_name": "General Model",
        "description": "General model with server-side context management",
        "endpoint": "openai"
      },
      "fallback": {
        "id": "your-fallback-model",
        "display_name": "Fallback Model",
        "description": "Balanced performance with server-side management",
        "endpoint": "openai"
      }
    },
    "default_settings": {
      "temperature": 0.2,
      "max_tokens": 4096,
      "top_p": 0.95
    },
    "research_settings": {
      "temperature": 0.1,
      "max_tokens": 4096,
      "system_prompt": "You are a research assistant with server-side context management."
    }
  },
  "cursor_integration": {
    "enabled": true,
    "model_mapping": {
      "research": "your-research-model",
      "general": "your-general-model"
    },
    "api_key": "your-proxy-api-key"
  },
  "claude_code_integration": {
    "enabled": true,
    "anthropic_base_url": "http://127.0.0.1:${PROXY_PORT}",
    "default_model": "your-general-model"
  }
}
