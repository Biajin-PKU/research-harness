name: research-harness
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - -e ./packages/paperindex
      - -e ./packages/research_harness[dev]
      - -e ./packages/research_harness_mcp[dev]
